# Reference for ~/apps/playr-api.env on the server.
# Referenced by /etc/systemd/system/playr-api.service via EnvironmentFile=
# and sourced by deploy/deploy-backend.sh for running EF Core migrations.
#
# WARNING: .github/workflows/deploy.yml OVERWRITES ~/apps/playr-api.env on every
# deploy to main, generating it from GitHub secrets. Edits made directly on the
# server will be lost on the next deploy. Change values in the workflow (for
# non-secret settings) or in the repository secrets (for secret ones).
#
# Required repository secrets:
#   SSH_PRIVATE_KEY, SSH_KNOWN_HOSTS  - deploy access to the server
#   DB_PASSWORD                       - password for the 'playr' postgres user
#   JWT_SIGNING_KEY                   - at least 32 characters
#   STEAM_API_KEY                     - Steam Web API key
#   SMTP_USERNAME                     - Brevo SMTP login, <digits>@smtp-brevo.com
#   SMTP_KEY                          - Brevo SMTP key, starts with xsmtpsib-
#
# This file is NOT committed to git (see .gitignore) - only this example is.

ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://127.0.0.1:5258
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=playr;Username=playr;Password=CHANGE-ME
Jwt__Issuer=PLAYR
Jwt__Audience=PLAYR
Jwt__SigningKey=CHANGE-ME
Jwt__ExpirationMinutes=60
Steam__ApiKey=CHANGE-ME
Steam__SyncIntervalHours=12

# Public URL of the frontend. Used to build email confirmation links.
Frontend__BaseUrl=https://www.playr.viktorradman.se

# SMTP settings for outgoing mail (email confirmation).
# Leave Email__Host empty to disable sending - links are then written to the log instead.
# Example provider: Brevo (smtp-relay.brevo.com, port 587).
Email__Host=
Email__Port=587
Email__Username=
Email__Password=
Email__FromAddress=no-reply@playr.viktorradman.se
Email__FromName=PLAYR
Email__UseStartTls=true

# Persistent storage for uploaded files (avatars, post media, etc.)
# Must live OUTSIDE the dotnet publish output directory, since `dotnet publish`
# wipes and recreates that folder on every deploy.
FileStorage__RootPath=/home/viktor/apps/playr-uploads

